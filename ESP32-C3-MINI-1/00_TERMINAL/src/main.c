#include <stdio.h>

void app_main() {

    // this example send a text through usb/serial debug port
    // on Plataformio tree click on Build, Upload then Monitor to see the text on Terminal
    printf("SEND A TEXT THROUGH USB DEBUG PORT \n");

}